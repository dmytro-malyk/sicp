«аметим, что наша модель вычислений разрешает существование комбинаций, операторы кото-
рых Ч составные выражени€. — помощью этого наблюдени€ опишите, как работает следующа€
процедура:

(define (a-plus-abs-b a b)
((if (> b 0) + -) a b))

ѕроцедура a-plus-abs-b принимает в качестве аргументов два числа a и b.
ƒалее вычисл€етс€ предикат (> b 0). 
≈сли b - €вл€етс€ положительным числом, возвращаетс€ значение следстви€ - в нашем случае это операци€ сложени€. ¬ыполн€етс€ сложение двух аргументов a и b.
≈сли b - отрицательное число, следовательно возвращаетс€ значение альтернативы - в нашем случае это операци€ вычитани€. ¬ыполн€етс€ вычитание числа b из числа a.